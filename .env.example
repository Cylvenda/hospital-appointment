# Server-only Django API URL used by the same-origin Next.js proxy.
# Vercel example: https://your-api.onrender.com/api/
BACKEND_API_BASE=http://127.0.0.1:8000/api/

# Usually leave this unset so browsers use /api/proxy/ and HttpOnly cookies.
# NEXT_PUBLIC_API_BASE=/api/proxy/
